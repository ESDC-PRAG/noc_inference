# con_noccai_v7 — inference dependencies
# Install into the jobpostings conda env:
#   pip install -r requirements.txt --break-system-packages
#
# PyTorch and CUDA must already be installed via conda
# (do not install torch here — use the conda nocml/jobpostings env).

transformers>=4.35.0
tokenizers>=0.14.0
sentencepiece>=0.1.99
safetensors>=0.4.0

numpy>=1.24.0
pandas>=2.0.0

# HTML cleaning
beautifulsoup4>=4.12.0

# Optional: faster tokenisation
# tokenizers is already a transformers dependency
